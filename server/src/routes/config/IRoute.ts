/* The code snippet is defining an interface named `IRouter` in TypeScript. This interface has a single
method `routes()` which does not take any arguments and returns `void`. The comment `// * Interfaz
IRouter` is likely a description or annotation for the interface. */
export interface IRouter {
    routes(): void;
}
